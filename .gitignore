# Environment files
.env
.env.local
.env.*.local

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Dependencies
node_modules/

# React Native (Expo)
app/ios/Pods/
app/ios/build/
app/android/.gradle/
app/android/app/build/
app/android/build/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
*.hprof

# Next.js
web/.next/
web/out/

# Dev certificates (generated by scripts/gen-dev-certs.sh)
app/dev-certs/

# Native build artifacts
native/c2pa-bridge/target/

# Progress deck (local only)
progress

# Misc
*.tsbuildinfo
.expo/
native/jarosz-wasm/target/
.claude

# Sensor session module build artifacts
app/modules/sensor-session/android/build/

# Python pipeline build artifacts
pipeline/**/__pycache__/
pipeline/**/.venv/
pipeline/**/*.pyc

# Solana / Anchor (Unit D 以降)
/target/
/.anchor/
test-ledger/
**/*.so
# program keypair はローカル保管。本番デプロイ前にチームで共有 keypair を作る
/target/deploy/*-keypair.json
# License Collection / USDC mint keypair も同じくローカル保管
keys/
# Anchor / license-cli が生成する bootstrap config (環境ローカル)
network.json
# setup スクリプトが devnet で生成する fixtures (= 各開発者ローカル artifact)。
# leaf owner の secret key を含むので絶対に commit しない。
tests/license-nft/fixtures.json
tests/license-nft/fixtures-canopy.json
tests/license-nft/node_modules/

# Hand-pose module build artifacts
app/modules/hand-pose/android/build/

# Pipeline node_modules
pipeline/**/node_modules/


version: '3.8'

services:
  # Next.js (Frontend + API Routes)
  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    networks:
      - rootlens-network
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next

networks:
  rootlens-network:
    driver: bridge

# privacy-blur (Android)

Android 実装は本ユニットでは未対応。SPECS §2.3 step 7 のオンデバイス顔/OCR ブラー処理は iOS でのみ完成済 (`../ios/`)。

iOS と同等の挙動 (MediaCodec + GLES + ML Kit Face Detection + ML Kit Text Recognition v2) は後続タスクで対応する。
